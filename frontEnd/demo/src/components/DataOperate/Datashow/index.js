import React, { Component } from 'react'
import { Button } from 'antd'

class Datashow extends Component{
    render(){
        return (
            <Button style={{minWidth:'206px',marginBottom:'10px'}}>show</Button>
        );
    }
} 

export default Datashow;