import React, {Component} from 'react'
import { withPropsAPI } from '@src';

class FeatureBinary extends Component{
    render(){
        return (
            <div>dd</div>
        );
    }
}

export default withPropsAPI(FeatureBinary)